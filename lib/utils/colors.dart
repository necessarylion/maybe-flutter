import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF1DE9B6);
  static const Color primaryAccent = Color(0xFF1DE9B6);

  // Background colors
  static const Color background = Color(0xFF181A20);
  static const Color cardBackground = Color(0xFF23262F);
  static const Color appBarBackground = Color(0xFF222C3A);
  static const Color bottomNavBackground = Color(0xFF222C3A);

  // Text colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFB0B0B0);
  static const Color textMuted = Color(0xFF9E9E9E);

  // Status colors
  static const Color positive = Colors.greenAccent;
  static const Color negative = Colors.redAccent;
  static const Color positiveLight = Color(0xFF4CAF50);
  static const Color negativeLight = Color(0xFFF44336);

  // Avatar colors
  static const Color avatarBackground = Color(0xFF424242);

  // Shadow colors
  static const Color shadow = Color(0x42000000);

  // Unselected navigation color
  static const Color unselectedNav = Color(0xFF9E9E9E);

  // Button colors
  static const Color addButtonBackground = Color(0xFF4A90E2);
}
